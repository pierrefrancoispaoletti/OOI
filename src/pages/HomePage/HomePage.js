import React from "react";
import { HomePageContainer } from "./homepage.styles";

const Homepage = ({ children }) => {
  return <HomePageContainer>{children}</HomePageContainer>;
};

export default Homepage;
